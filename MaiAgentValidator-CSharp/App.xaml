<Application x:Class="MaiAgentValidator.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Material Design Themes -->
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="Blue" SecondaryColor="Amber" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
                
                <!-- Custom Styles -->
                <ResourceDictionary>
                    <!-- Card Style -->
                    <Style TargetType="{x:Type materialDesign:Card}">
                        <Setter Property="Padding" Value="16"/>
                        <Setter Property="Margin" Value="4"/>
                        <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth2"/>
                    </Style>
                    
                    <!-- Button Styles -->
                    <Style x:Key="PrimaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                        <Setter Property="Margin" Value="4"/>
                        <Setter Property="MinWidth" Value="88"/>
                        <Setter Property="Height" Value="36"/>
                    </Style>
                    
                    <Style x:Key="SecondaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
                        <Setter Property="Margin" Value="4"/>
                        <Setter Property="MinWidth" Value="88"/>
                        <Setter Property="Height" Value="36"/>
                    </Style>
                    
                    <!-- TextBox Styles -->
                    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
                        <Setter Property="Margin" Value="0,4"/>
                        <Setter Property="MinHeight" Value="40"/>
                    </Style>
                    
                    <!-- PasswordBox Styles -->
                    <Style TargetType="{x:Type PasswordBox}" BasedOn="{StaticResource MaterialDesignOutlinedPasswordBox}">
                        <Setter Property="Margin" Value="0,4"/>
                        <Setter Property="MinHeight" Value="40"/>
                    </Style>
                    
                    <!-- ListBox Styles -->
                    <Style TargetType="{x:Type ListBox}" BasedOn="{StaticResource MaterialDesignListBox}">
                        <Setter Property="Margin" Value="0,4"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
                    </Style>
                    
                    <!-- DataGrid Styles -->
                    <Style TargetType="{x:Type DataGrid}" BasedOn="{StaticResource MaterialDesignDataGrid}">
                        <Setter Property="Margin" Value="0,4"/>
                        <Setter Property="AutoGenerateColumns" Value="False"/>
                        <Setter Property="CanUserAddRows" Value="False"/>
                        <Setter Property="CanUserDeleteRows" Value="False"/>
                        <Setter Property="IsReadOnly" Value="True"/>
                        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                        <Setter Property="HeadersVisibility" Value="Column"/>
                    </Style>
                    
                    <!-- ProgressBar Styles -->
                    <Style TargetType="{x:Type ProgressBar}" BasedOn="{StaticResource MaterialDesignLinearProgressBar}">
                        <Setter Property="Height" Value="6"/>
                        <Setter Property="Margin" Value="0,8"/>
                    </Style>
                    
                    <!-- Slider Styles -->
                    <Style TargetType="{x:Type Slider}" BasedOn="{StaticResource MaterialDesignSlider}">
                        <Setter Property="Margin" Value="0,8"/>
                    </Style>
                    
                    <!-- CheckBox Styles -->
                    <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource MaterialDesignCheckBox}">
                        <Setter Property="Margin" Value="0,4"/>
                    </Style>
                    
                    <!-- TabControl Styles -->
                    <Style TargetType="{x:Type TabControl}" BasedOn="{StaticResource MaterialDesignTabControl}">
                        <Setter Property="TabStripPlacement" Value="Top"/>
                    </Style>
                    
                    <!-- ScrollViewer Styles -->
                    <Style TargetType="{x:Type ScrollViewer}">
                        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                        <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
                    </Style>
                    
                    <!-- Custom Converter for Boolean to Visibility -->
                    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
                    
                    <!-- Animation Styles -->
                    <Style x:Key="FadeInAnimation" TargetType="{x:Type FrameworkElement}">
                        <Style.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                                       From="0" To="1" Duration="0:0:0.3"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Style.Triggers>
                    </Style>
                    
                    <!-- Status Colors -->
                    <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
                    <SolidColorBrush x:Key="ErrorBrush" Color="#F44336"/>
                    <SolidColorBrush x:Key="WarningBrush" Color="#FF9800"/>
                    <SolidColorBrush x:Key="InfoBrush" Color="#2196F3"/>
                    
                    <!-- Typography Styles -->
                    <Style x:Key="TitleTextBlock" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MaterialDesignHeadline5TextBlock}">
                        <Setter Property="Margin" Value="0,16,0,8"/>
                        <Setter Property="FontWeight" Value="Medium"/>
                    </Style>
                    
                    <Style x:Key="SubtitleTextBlock" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MaterialDesignHeadline6TextBlock}">
                        <Setter Property="Margin" Value="0,12,0,6"/>
                        <Setter Property="FontWeight" Value="Regular"/>
                    </Style>
                    
                    <Style x:Key="BodyTextBlock" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MaterialDesignBody1TextBlock}">
                        <Setter Property="Margin" Value="0,4"/>
                        <Setter Property="TextWrapping" Value="Wrap"/>
                    </Style>
                    
                    <Style x:Key="CaptionTextBlock" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MaterialDesignCaptionTextBlock}">
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBodyLight}"/>
                    </Style>
                    
                    <!-- Layout Styles -->
                    <Style x:Key="SectionPanel" TargetType="{x:Type StackPanel}">
                        <Setter Property="Margin" Value="0,16"/>
                    </Style>
                    
                    <Style x:Key="FormGrid" TargetType="{x:Type Grid}">
                        <Setter Property="Margin" Value="0,8"/>
                    </Style>
                    
                    <!-- Loading Indicator -->
                    <Style x:Key="LoadingIndicator" TargetType="{x:Type ProgressBar}" BasedOn="{StaticResource MaterialDesignCircularProgressBar}">
                        <Setter Property="Width" Value="24"/>
                        <Setter Property="Height" Value="24"/>
                        <Setter Property="IsIndeterminate" Value="True"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application> 